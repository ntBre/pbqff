fn main() {
    // if config.optimize {
    //     match std::fs::create_dir("opt") {
    //         Ok(_) => (),
    //         _ => (),
    // Err(e) => {
    //     if e.kind() == ErrorKind::AlreadyExists {
    //         eprintln!("directory `opt` already exists");
    //         std::process::exit(1);
    //     } else {
    //         panic!();
    //     }
    // }
    // };
}
